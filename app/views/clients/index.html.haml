%h1 Listing clients

%table
  %tr
    %th Name
    %th Active
  - @clients.each do |client|
    %tr
      %td= link_to client.name, client
      %td= yesno client.active

%br

= link_to 'New client', new_client_path
