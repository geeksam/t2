%h1 Editing project

- form_for(@project) do |f|
  = f.error_messages
  = render :partial => 'form', :locals => {:f => f}
  %p= f.submit 'Update'

%br
= link_to 'Show', @project
|
= link_to 'Back', projects_path
