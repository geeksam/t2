<h1>Editing task</h1>

<% labeled_form_for(@task) do |f| %>
  <%= f.error_messages %>

  <%= render :partial => 'form', :locals => {:f => f} %>

  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= separated_group do |g|
  g << link_to('Show', @task)
  g << link_to('Back', tasks_path)
end %>
